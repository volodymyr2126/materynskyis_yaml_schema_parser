schema:
  name: myschema
  tables:
    user:
      columns:
        id:
          type: int
          mode: required
        name:
          type: str
          mode: required
        avg_grade:
          type: float
          mode: nullable
    subjects:
      columns:
        id:
          type: int
          mode: required
        name:
          type: str
          mode: required
        hours:
          type: int
          mode: nullable
        number_of_credist:
          type: float
          mode: nullable
